@page "/counter"
@using Radzen.Blazor

<h1>Counter</h1>
<p>Current count: @currentCount</p>
<RadzenButton Text="Increment" Click="@IncrementCount" />

@code {
    private int currentCount = 0;
    private void IncrementCount()
    {
        currentCount++;
    }
}